# 2026-05-10

## ServerGuardian AI-Agent 实现

### 后端 (backend/src/guardian/)
- **已完成模块**：types.ts, ProcessManager.ts（已有），新增 LogParser.ts, CrashDetector.ts, AIAdvisor.ts, SafeExecutor.ts, RollbackManager.ts, Reporter.ts, index.ts（GuardianController）
- **集成到 core.ts**：添加 guardian 属性、initGuardian()/setupGuardianWSHandler()/setupGuardianRoutes() 方法
- **扩展 ws.ts**：添加 guardianLog/guardianStatus/guardianCrashDetected 等 9 个 WS 事件方法
- **更新 config.ts**：IConfig 添加 guardian 配置项 + 默认值 + 环境变量覆盖

### 前端
- **新增 GuardianView.vue**：完整守护者面板，含实时日志、崩溃诊断、操作确认、统计面板、设置面板
- **侧边栏添加 "AI 模式"**：在 App.vue 中新增 guardian 菜单项，使用 RobotOutlined 图标
- **路由配置**：router.ts 添加 /guardian 路由
- **国际化**：8 种语言文件全部添加 "guardian" 菜单项

### 修复与增强
- 修复 v-model 三元表达式编译错误（`Collapse :activeKey` 改用计算属性）
- 修复 5 处 TypeScript 编译错误（AIConfig 导入、`null`/`undefined`、类型收窄等）
- 增加：启动配置对话框（目录、命令、服务端类型）
- 增加：启动前检查 AI 配置（OpenAI 未填 key / Ollama 未填地址时阻止）
- 增加：设置面板中"测试 AI 连接"按钮（向 AI 发 Hello 测试，实时返回结果及延迟）
- 增加：设置面板按钮布局改为 flex 行排列
